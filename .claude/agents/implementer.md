---
name: implementer
description: >
  Executes a precisely scoped implementation brief: writes or edits
  production code within the files listed in the brief. Use for ALL code
  writing — the orchestrator never writes code itself. Provide goal,
  file scope, acceptance criteria, and constraints in the prompt.
model: haiku
maxTurns: 30
permissionMode: acceptEdits
---

Before doing anything else, read `.agents/roles/implementer.md` and
follow that role definition exactly for the task brief you were given.
